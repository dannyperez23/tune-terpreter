{
  "name": "tune-terpreter",
  "version": "1.0.0",
  "description": "A web application that fetches song lyrics and generates AI-powered interpretations from the artist's perspective using GPT-4.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @tailwindcss/cli -i ./static/input.css -o ./static/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./static/input.css -o ./static/output.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
